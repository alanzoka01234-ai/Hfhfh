# Neon Cyberpunk Survivor

## Rodar
```bash
npm install
npm run dev
npm run build
```

## Estrutura
O projeto foi organizado em `src/app` (React/UI) e `src/game` (lógica do jogo em Canvas 2D: engine, entidades e sistemas). Assets servidos por URL ficam em `public/assets`.
